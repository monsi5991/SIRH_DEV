services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: sirh
      POSTGRES_PASSWORD: sirh
      POSTGRES_DB: rbac_demo
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
